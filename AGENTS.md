# 真题句读协作规则

本文件适用于整个项目。任何 GPT、Codex 或人工协作者开始工作前，必须完整阅读：

1. `START_HERE.md`
2. `docs/CONTENT_QUALITY_STANDARD.md`
3. `docs/ARTICLE_IMPORT_TEMPLATE.md`
4. `docs/RELEASE_CHECKLIST.md`

## 不可降低的边界

- 2000年完形是词库完整性基线；2010 Text 1是新阅读训练模板，见 `docs/TRAINING_TEMPLATE.md`。要求准确和可迁移，不要求每句同等长度。简单句只讲一个关键点，复杂句再展开多层关系。
- 一次只新增一篇文章；不得顺手重构界面、登录、同步、复习算法或现有精审内容。
- 以用户提供的真题原文为唯一文本依据。不得自行补句、改写题干或猜测缺失文字。
- 不得使用“后续补充”“结合本句理解”“待精审”“暂无资料”等占位内容。
- 不得创建空链接。任何可点击词、词组、近义词或同源词都必须立即显示有意义的信息。
- 词组必须同时保存原文表达和规范原型，例如：
  `between his consumption and his production` → `between A and B`。
- 搭配、结构、例句、近义词区别必须有中文解释；不能只列英文标签。
- 通用“考研近义词”只负责辨析；“本句可替换”必须另存为句子语境数据，包含完整改写句、中文义、语气差别和可点击目标。
- 新增句子必须填写 `beginnerSyntax`：逐组说明构成形式、具体句法功能和修饰对象；每个从句必须给出类型、引导词、内部主谓及具体 `predicateDetails`（宾语、表语或宾补）和翻译顺序。先解释关系，再给术语；禁止只写“修饰语”或“从句”。
- 新增正文/翻译句还须填写 `beginnerSyntax.reading`：一句话点明真正阅读难点，1—3个带原文证据的问题讲清为什么这样拆。嵌套修饰用成分 `children` 保存父子关系；不能把含宾语的整段统一标成谓语，也不能用关键词自动猜修饰对象。示例见2010 Text 1；既有内容升级范围见 `docs/SYNTAX_UPGRADE_PROGRESS.md`，不能把换界面等同于全库重新精审。
- `trunk` 只能从原句删去修饰成分，不得用释义改写或加入原文没有的英文词；复杂句中的并列实例和从句必须分别保存准确边界。
- 语义不确定时写入交付报告的“阻塞与交接”部分，不得把猜测写入正式数据。
- 不得写入或索取正式网站身份、账号密码、验证码、邮件密钥、数据库内容或部署令牌。
- 已授权通过既有 GitHub `main` → Cloudflare 流程发布精审内容。另建部署、改域名、改访问权限或接入新服务须单独授权。

## 固定工作顺序

1. 原文核对：逐字核对正文、题干、选项、标点和题号。
2. 句子拆分：保存原句、彩色成分、主干、零基础成分组合、从句内部主谓宾、逐层结构、语法、逻辑、结构直译和通顺译文。
3. 词汇覆盖：扫描正文、题干和选项中的全部英文词形，逐个检查卡片字段。
4. 词组覆盖：只标记应整体理解的表达，同时保存来源表达与规范结构。
5. 题目解析：答案、定位、全部真实选项理由必须互相一致。原卷四选一保留 A—D 四项；原卷 T/F 判断题严格保留 T/True、F/False 两项，不虚构选项。
   新阅读模板还须一起完成原卷段落、文章地图、指代/时间关系、逐句主动任务和多句题目证据。使用稳定语法概念ID；不能把查看或译文自评算作独立掌握。其他题型按任务本身训练，不强套阅读选择题。
6. 全年索引：确认新增内容进入年度单词表、词组表和出现次数统计。
7. 按 `docs/RELEASE_CHECKLIST.md` 执行精简审计：普通内容导入运行 `npm run quality:content` 和 `npm run build`；通过后不重复运行完整套件。失败后只重跑受影响检查，不得删除、跳过或放宽断言。
8. 填写 `docs/CHANGE_REPORT_TEMPLATE.md`，记录实际检查、来源疑点和交付状态。语义自审随录入完成，不再另做多轮全量复读。

## 当前持续导入授权

- 最新优先级（2026-09-17）：暂停批量迁移，2010 Text 1语法模型、阅读入口、篇章地图、题目证据与主动练习已分批提交；实际界面验收状态见升级台账。允许为此修改公共展示和学习状态，但不得碰正式个人数据。Text 2只有逐句关系已补充，尚未完成新模型和训练层；模板验收后先补Text 2，再接Text 3。不能误称全库已升级。
- 新模板必须将 `visualRole` 配色与 `grammarFunction` 精确语法分离；使用 `withReviewedSyntax` 复用人工成分名称、范围和关系，不各写一套。主系表中的表语不得标成宾语，时间/原因/条件从句不能因同色而使用错误名称。

- 2026-09-17 用户明确要求所有已导入文章都升级句法教学，并修复单词释义混入词组/整句意思的问题；范围是当前35个模块，不只2010 Text 1。允许为此修改公共解析和展示；按篇独立提交推送，不等待人工复核。进度必须记入 `docs/SYNTAX_UPGRADE_PROGRESS.md`，新导入仍与既有内容优化分开提交。
- 单词的 `contextualMeaning` 只写该词在当前语境的义项或语法作用；修饰语、宾语、整句事件与习语整体翻译写入 `use` 或词组条目。代词可以说明指代，习语成分应解释其作用，不可为了简短而误换成无关字面义。

- 2026-09-17用户继续提供2012年英语二整卷，授权沿用2011质量完成九个模块。按篇独立精审、提交、推送；不修改账号、同步和部署配置。原图为员工满意度表格，数值按原卷读取；Text 2第145段的异常并列形式须以原卷`between girls as not only innocent but as evidence of innocence`原样保留，不得按猜测补写。

- 2026-09-17 用户另行提供2011年英语二整卷并授权完成全部试卷：完形、Text 1—4、Part B、整篇英译汉和两道写作。仍按篇独立提交发布；2011 Part B必须真实保留A—G七选项对应题，写作保留原图与原要求，不伪造选择题或评分。

- 用户已授权从 2000 年持续处理到 2010 年，不再逐篇等待用户人工复核。2026-09-16 起采用精简审计，内容字段与准确性标准不变，不重复逐句审计无变化的旧文章。
- GitHub 仓库是唯一代码源。每篇文章必须对应一个独立提交；发布清单要求的检查通过后，才可推送到 `origin/main`，由既有 Cloudflare 流程自动部署。权限失败或分支保护要求审批时停止，不得绕过。
- 每篇完成后更新 `docs/IMPORT_PROGRESS.md`，并在 `docs/change-reports/` 新建该篇独立交付报告。不得复用上一篇报告或把多篇合在一次提交中。
- 后续遇到已有单词、词组或词族时，必须复用稳定规范词条，并补充新语境、出现位置和统计；禁止复制出同义的平行词条。
- 重复词的本句义和本句用法必须按 `articleId + sentenceId + headword` 解析；不得用最早导入文章的释义覆盖其他文章，也不得因同篇重复而强行共用一个本句义。
- 每年维护独立核验答案清单并由测试比对；答案修订必须记录可靠来源、定位和四项理由。
- 若缺少用户提供的某年可靠原文，应停在该年开始前索取原卷；不得为保持连续进度而使用来源不明的网络转录文本。
- 具体下一篇以 `docs/IMPORT_PROGRESS.md` 的当前接续点为准。原卷附件不保证随仓库克隆，执行者须先确认自己实际能读取文件。

## 修改要求

- 保留现有技术结构、视觉层级和移动端交互。
- 数据优先放入现有 `app/data.ts`、`app/lexicon.ts`、`app/knowledge-base.ts` 体系；首次拆分年度文件时必须保持兼容并补充测试。
- ID 必须稳定、唯一，便于个人笔记和复习记录长期关联。
- 不得用删除内容、取消点击、隐藏报错或弱化断言的方式让检查通过。
- 提交前说明修改文件、文章范围、已运行检查和仍需确认的项目。
