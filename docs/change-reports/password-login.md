# 邮箱密码登录交付记录

日期：2026-09-16。范围为用户明确授权的密码功能、原账号兼容与既有环境上线；没有导入新文章、改变答案、邮箱允许范围或部署身份。

## 功能与修改

- `app/api/_lib/password.ts`：8—128字符、随机盐及版本化PBKDF2密码凭证，无复杂度组合要求。
- `db/schema.ts`、`drizzle/0001_password_login.sql`和迁移元数据：新增独立`user_passwords`，不回填或改写原用户及学习记录，迁移可重复执行。
- `app/api/auth/password/route.ts`：密码登录和当前会话设置密码；检查来源、既有邮箱权限、输入及原子尝试限制。复用原账号和Cookie。
- `app/api/_lib/auth.ts`及session/verify-code路由：共用会话创建；验证码继续可用；邮件、密码能力分开报告，缺少新表不丢失旧会话。
- `app/study-app.tsx`、`app/globals.css`：电脑/手机双登录方式、设置/更新密码、确认输入、恢复提示、等待/成功/失败状态、关闭时清除输入。
- `app/study-sync.ts`：登录先读取云端，失败不上传；未变更记录不刷新修改时间，保留读取期间较新的本机编辑，支持重试与恢复网络后重新读取。
- `scripts/deploy-cloudflare.mjs`、`package.json`、`README.md`：先核验既有Worker/D1，再执行本次增量SQL，成功后才部署。首次设置和忘记密码说明已同步。

## 实际验证

| 检查 | 结果 |
| --- | --- |
| `node --test tests/password.test.mjs` | 4/4：长度、空白、随机盐、错误凭证及原数据/外键/重复迁移 |
| `node --test tests/auth.test.mjs` | 7/7：真实Workers运行时及隔离D1，设置/更新、原记录、未知/错误/越权、并发限流、验证码及缺表兼容 |
| `node --test tests/study-sync.test.mjs` | 3/3：稳定修改时间、读后比较、失败阻止上传 |
| `npm run lint` | 通过；后续账号小调整文件级复核通过 |
| `npm run quality:content` | 22/22；文章内容未改变 |
| `npm run build` | 通过；仅手机留白/提示收尾后重建受影响最终产物 |
| 构建后UI/产物测试 | 5/5 |
| 认证目录安全扫描 | 7个文件，未发现规则命中；另人工检查权限、输入、Cookie及敏感数据路径 |
| 发布目标`--check` | 既有english-exam / zhenti-judu-prod；检查本身没有执行生产SQL |

浏览器在独立上下文使用合成账号和拦截接口验证前端，不操作用户现有浏览器学习数据：电脑读取失败期间0次学习记录PUT，重试后恢复并上传原云端笔记；密码确认错误、成功清空及不入localStorage均验证。390×844手机完成错误密码→验证码→原记录恢复→设置入口，关闭后密码清空；账号面板无横向溢出。前端拦截检查不替代上表真实Workers接口测试，也不代表用户的真实账号已经设置密码。

## 类型检查及已知限制

额外运行`tsc --noEmit --incremental false`未通过。用TypeScript编译器对HEAD原代码作只读内存覆盖核验，原版本同样存在10项诊断：旧词库重复键、词库类型字段、Cloudflare运行时类型声明和既有Vite配置类型。当前未增加该组诊断；没有把全量类型检查写成通过，也没有修改这些范围外内容。

同步仍以整份记录时间比较，不包含多设备并发编辑的逐项合并/冲突历史，后续体验审查应单独评估。构建仍提示主学习包较大，不在密码提交中拆分知识库。

安全决策：只增加基础加盐密码哈希、同源设置、原账号权限和轻量限流，不引入认证平台；不保存或索取明文密码、验证码、部署令牌或生产数据库内容。持续被尝试的账号可能暂时限流，可用验证码恢复。密码更新不强制退出所有已存在会话。

## 发布与交接

提交时状态：本地实现与门禁已完成，等待独立提交、推送及线上核验。部署须在既有Cloudflare Builds权限内完成本次增量SQL；权限失败立即停止，不另建环境或跳过迁移。Git推送成功不能独自证明上线。

后续消息记录实际提交SHA与线上检查结果，不为将本提交SHA写回本文件反复amend。用户首次使用应自行通过验证码登录后在账号面板设置密码，不在聊天中提供实际密码或验证码。

### 首次发布核验后的迁移修正

功能提交`710428a`已推送。线上新密码路由及新界面资源可核验，但session返回`passwordConfigured=false`；空请求校验分别返回400/401，说明密码基础配置已启用，凭证表查询仍不可用。推测实际Cloudflare部署命令仍为默认Wrangler而未经过npm部署脚本，不能把此时状态记为密码上线成功。

本机没有已配置的Cloudflare API令牌或默认Wrangler登录文件，原生迁移尝试还遇到本机esbuild的spawn EPERM，没有成功执行生产SQL。未索取或读取任何真实凭证。

为让实际构建入口执行同一迁移，在`npm run build`的postbuild增加Cloudflare main专用门禁：只在官方注入的`WORKERS_CI=1`且分支为main时运行原幂等SQL，其他构建不连接生产库；不在HTTP接口里偷偷建表。若既有CI凭据缺少D1权限仍会停止，须由部署管理员在控制台处理，不绕过权限。新增2项部署守卫测试覆盖跳过边界和只读目标核验。

### 最终线上核验

迁移修正提交`2878a809736055fa294a7006c3875ab70bc8b4f0`已推送。2026-09-16晚，两个既有生产域名均返回HTTP200，匿名session为`configured=true`、`passwordConfigured=true`，此前false的凭证表能力检查已恢复。线上浏览器实际可见密码/验证码切换和恢复入口，新密码路由空输入400、未登录设置401。线上资源包含本次密码及读后写同步实现；没有设置用户实际密码或访问生产学习记录。

GitHub提交状态API仍受403限流，没有拿到Cloudflare控制台的对应SHA状态回执。功能生效由线上资源、路由、界面及新表能力共同核验，不冒充获得了控制台部署日志。Git推送后HEAD与origin/main差异为0/0。

上线后体验及知识库审查见`docs/change-reports/user-experience-audit.md`。其中其他问题是后续建议，未混入本次密码代码。
