# V2 标记与单词点击修正

## 用户问题与范围

用户指出重复点击无法清楚区分“选择词/词组”和“误触后取消”。本次仅修改V2交互，不新增文章，不修改原文、译文、答案、V1页面、词典义项、登录、部署或同步协议。

## 实现

- 做题页提供连续阅读、标单词、标词组、标句子四种明确模式。全文只有一个临时预选，点词/句不保存；蓝色虚线预选和黄色已存标记分开，确认按钮才写入。
- 单词一次点击即选中；词组先选起点再选终点，可重选起点及修正终点；切到另一来源重新选起点，不跨正文/题干/选项拼范围。
- 取消选择/Escape只丢弃预选；已标记列表提供明确来源和取消按钮。添加与移除后支持撤销上次操作，检查记录updatedAt和active，拒绝覆盖之后发生的修改。
- 取消、撤销都保留稳定ID和createdAt；取消使用active:false，不删除同步历史。重复确认相同动作不反向切换。
- 快速读懂的所有英文词可点击，通过精确sourceId打开原有词卡。保留空格、标点和原文词形；点击不加入记忆，关闭不会标记整句。待学、复习仍用共用词卡的显式入口。
- 新逻辑拆到 `app/article-v2/source-selection.ts`、`source-marking.tsx`；未向study-app.tsx继续添加功能，未新增存储字段。

## 验证与发布

开发专项22/22通过，包括35篇V1对象/SSR冻结基线、2013原卷、题目隐藏、精确来源、预选/修正、明确移除、撤销冲突保护与同步信封。ESLint零错误，保留6条原有warning。TypeScript独立检查仍为此前10项基线诊断。

发布检查：`npm run quality:content` 193/193通过；词汇审计3053词，精审输入未变化；`npm run build`通过。产物、HTML、同步及UI共28项中，首次27项通过，1项旧年份断言遗漏已上线的2013。将完整预期年份明确更新为2000、2001、2010、2011、2012、2013，保留2012原图和数据全部断言，受影响UI文件重跑14/14通过；其余14项此前通过，无须重跑。未修改旧文章或冻结快照。

发布按用户已明确的直接提交授权沿用既有main→Cloudflare流程；线上实测结果由交付消息及Git历史记录，不把未测360px/390px手机触控或离线重新打开记为通过。独立TypeScript检查的10项旧诊断涉及Cloudflare类型、lexicon、Vite与worker类型，本次未扩大修改范围。

## 正式站交互实测与语境修正

交互提交 `a25689e6d9eca71b2791aa195b57381dd99e515e` 已进入main；正式站 https://onehjt.dpdns.org/ 实际出现新菜单。浏览器访客状态验证：预选textile时计数仍为0，取消不保存，确认计数为1，撤销恢复0；确认后刷新仍为1，重选显示“取消此标记 / 保留标记”，保留不移除，明确取消恢复0。词组起点keep、终点from正确预览keep the man away from，取消不保存。测试标记最终已取消，未登录或操作正式个人账户。

快速读懂点击textile打开共享词卡并定位2013第1句。此时发现该词用法仍回退为其他文章的“与cars并列作主语”，因此只补2013正文第1句和21题C项的textile形容词语境及优先搭配textile mill(s)。不修改全局词条、任何V1语境或原文。补充精确词性、词义、用法、搭配顺序断言；受影响V2/试点及35篇V1冻结基线22/22通过，生产构建与受影响文件ESLint再次通过。手机触控、360px/390px和离线重新打开仍未实测。
