# 返回原题、可辨识复盘与接手规则

范围：2010 Text 1样板及共用训练展示。未迁移Text 2、Text 3或其他年份。

## 修改

- 证据跳转保留题号、具体选项（从该项进入时）、已展开证据区和滚动位置；精读显示“返回第X题”。
- 复盘卡显示年份/篇目/句序、原句开头、具体历史错误与时间、下次日期；可按今日到期、仍然答错、曾借助提示、篇目、概念筛选，并进入指定任务。
- 精读内层改名为“纯净原句｜查词｜看结构”。移动布局补充可换行控件、最小点击高度、窄屏单列连接与筛选、16px输入及底部返回条留白。
- 词汇优先级明确为编辑建议，打开词卡不会自动入队；专名建议不单独背词，功能词建议回到句法任务。手动标记规则不变。
- 第二句删除“对象性主语”；第一句译文明确为拍卖作品而不是仅“亮相”。原文、选项、题号不变。
- 更新接手入口、质量标准、模板、发布清单和进度；废止全句提示扩散、历史答过永久辅助和只看覆盖的定位规则。

## 已执行检查

- `npm run quality:content`：44项通过。
- `npm run build`：通过。
- `npm run lint`：无错误，保留原有img提示。
- `node --test --test-concurrency=1 tests/ui-components.test.mjs tests/rendered-html.test.mjs`：14项通过。
- TypeScript补检：本轮修改模块无错误；全库旧基线错误仍在，不能写全量通过。
- 公开主站已确认第三组生成型任务：先看地图再找ended为独立答对；继续划完整主语也为独立答对，没有被上一题反馈无条件污染。匿名操作，不登录个人账号。
- 功能提交`25a5c22a3f1a69f121ecdc700aa9378325ed956e`的Cloudflare Workers Builds状态已读取：completed/success；正式入口`https://onehjt.dpdns.org/`刷新后看到“纯净原句”等本组特征。
- 第24题选择单句范围但全选19句：覆盖100%、精确度16%、明确判范围过宽；交卷后重做只选第18句：覆盖/精确度100%，保留2次记录；刷新后仍为2次。
- 第23题B项“比较的是关注度”跳到第8句，出现“正在核对：第23题 B项”；返回后仍展开逐项证据。稳定位置实测跳转前后scrollY同为3344，按钮top同为446.47px。
- 复盘切换“全部已练任务”显示“2010 Text 1 · 第1句”、原句开头、3天后的日期；点击主语范围任务直接打开该任务，未回到默认的任务1。
- 桌面截图已检查：当前浏览器宽1363px、内容视口1348px、文档宽1348px，无桌面横向溢出。该结果不代表手机宽度通过。

## 环境边界与接续

当前可用浏览器为桌面Chrome，未提供视口切换或Android/iOS设备。本轮未完成360px/390px渲染、Android Chrome/iOS Safari真机触控、软键盘遮挡与手机性能检查。CSS点击区和组件检查不能证明这些项目通过。

先完成样板剩余交互验收，再按既定顺序补齐2010 Text 2完整训练层。未接入的Text 3草稿保持原状，不属于交付。无需再等用户逐篇人工批准。
