# 2013英语二 Text 1：V2正式试点

## 范围与来源

- 唯一生产试点：`2013-p1`，阅读Text 1，21—25题；未导入同卷其他内容。
- 用户文件：`考研英语二2013年真题（整卷）.docx`；SHA256 `7f00fcff824e1a0c21261d1f4d13e742b8f173748f56ec50abf275b143e75131`。
- 正文来自DOCX提取段落114—119，题干与选项来自120—144。6个原卷段落重组为14个稳定句源；原卷快照为 `tests/fixtures/2013-passage-1-source.json`。
- 答案按原选项顺序为21—25题 A/D/B/B/C；第三方答案速查只作字母交叉核对，逐题判断仍以用户原卷证据为准。

## 内容判断

- 每句均有一句话意思、连续最小分块、当前障碍和段落作用；普通事实句不生成完整语法树。
- 仅1、3、6、7、8、11、13、14句因题目证据、嵌套、省略、指代、数字范围或全文主线进入不同深度；其中3、8、11、14句有完整关系结构。全篇只有2项主动任务，分别服务替代方向和教育范围，不按句凑题。
- 五题均保存最小充分证据、正确项同义转换、三个错误项各自的关键差别和独立短再判断。提交后按用户错题优先，不把完整标准讲义放在首屏。
- 词汇来源覆盖正文、题干和选项；`average`、`note`、`relate`、`field`、`over`、`bill`等按当前语境区分，固定搭配作为独立记忆对象。2013词组知识按文章作用域解析，不写入会改变V1渲染的全局别名。

## 兼容边界

- 只有 `experienceVersion: 2` 路由到四页；35篇V1没有新增版本字段。
- V1冻结基线持续验证对象序列化及read/words/structure展开/折叠和题目卡SSR；新增2013词组解析不得影响旧文章。
- 作答、任务、词汇继续写既有字段；V2页面/标记/追问使用可选字段和原同步保护，不创建平行词库或新服务。

## 验证

- `tests/2013-passage-1-v2.test.mjs`：原卷重组、题目与答案、明确路由、内容门禁、浅/深层、做题页泄漏、错因顺序、多义与独立词组。
- `tests/article-v2.test.mjs`：35篇V1冻结、四页工程边界、保存与同步兼容。
- 真实试点、V1冻结、同步及认证定向回归：52/52通过。
- `npm run quality:content`：189/189通过；随后词义账本检查退出0，共3053词条、5303展示行，2013新增多义决定已经人工写入原有账本。
- `npm run build`：五阶段生产构建完成并输出 `Build complete.`；本地postbuild按设计跳过远端迁移。
- `npm run lint`：退出0、零错误；仅保留6条与本次无关的旧warning，新增的未使用导入已删除。
- TypeScript独立检查仍有10项旧基线诊断（Cloudflare类型、既有词典类型与Vite配置）；本次仅因导入行增加产生行号偏移，没有新增诊断，不写成类型检查通过。
- `git diff --check`：通过。

## 当前未验证

受控浏览器实际尝试访问本机 `127.0.0.1:5173`，被浏览器策略以 `net::ERR_BLOCKED_BY_CLIENT` 拒绝。因此360px与390px真实浏览器无横向溢出、触控标记与返回路径、离线重开均未验证；CSS规则、SSR和构建通过不能替代这些结论。正式站未部署本分支，也没有对应线上SHA；未完成前只推独立审阅分支，不合并生产main。
