# 2012英语二整卷：公开页面验收

## 范围和结论

- 日期：2026-09-17。本记录只补充公开页面验收与发布状态，不更改试题、账号、学习同步或部署配置。
- 2012年已完成的八个模块均已独立提交至`main`：完形`ebf3d9c`、Text 1`b9d4dd3`、Text 3`74962f6`、Text 4`f23c7d3`、Part B`e27bb20`、英译汉`6858d07`、写作47`3ad8559`、写作48`0b1768e`；年份下拉入口修复为`657a967`。
- 主站`https://onehjt.dpdns.org/`实际打开并显示2012，八个完成模块均可进入。该结论来自匿名浏览器的实际页面，而非仅依据本地构建或Git推送。
- 阅读Text 2（26—30）未导入：用户原卷第145段XML与提取文本均为`between girls`，缺少关键并列对象。不得将公开异文静默补入用户原卷，因此整卷不能误称为九个模块全部完成。
- 既有入口`https://english.onehjt.dpdns.org/`仍显示旧版（下拉最高2011、2000年目录），尚未同步到当前Worker。受保护的直接发布命令在检测到当前环境没有`CLOUDFLARE_API_TOKEN`后于迁移前停止；没有执行远端D1迁移、没有写入学习数据，也没有绕过该门禁。

## 浏览器隔离边界

使用未登录浏览器页面访问公开站点，并在请求层只放行`GET`、`HEAD`和`OPTIONS`；所有非只读`/api/`请求均设置为拦截。验收期间未打开个人账号、未读取云端学习数据、未提交作答、未写入任何账号或数据库记录。

## 主站公开内容核验

| 模块 | 实际句数 | 题号/任务 |
|---|---:|---|
| 完形填空 | 16 | 1—20 |
| 阅读Text 1 | 18 | 21—25 |
| 阅读Text 3 | 24 | 31—35 |
| 阅读Text 4 | 18 | 36—40 |
| 阅读Part B | 28 | 41—45 |
| 英译汉 | 6 | 46，整篇15分 |
| 写作Part A | 6条原题指令 | 47，10分 |
| 写作Part B | 4条原题指令 | 48，至少150词、15分 |

合计120句（含10句写作指令）、40道客观题及46—48三道主观任务。年份下拉实际显示2000、2001、2010、2011、2012；选择2012后目录恰为以上八个模块，不展示来源不完整的Text 2。

写作48页面实际显示用户原卷表格图片，图片说明与图中数据一致：满意/不清楚/不满意三列，≤40岁为16.7%/50.0%/33.3%，41—50岁为0.0%/36.0%/64.0%，>50岁为40.0%/50.0%/10.0%。自测模式只有一个“我的英文作文”输入框；空白时显示“当前0词 · 原题要求至少150词”，提交按钮保持禁用，未在提交前暴露教学范文。

## 本轮检查与交接

| 检查 | 实际命令/范围 | 结果 |
|---|---|---|
| 年份入口与多义词回归 | `node --test --test-concurrency=1 tests/ui-components.test.mjs tests/vocabulary-senses.test.mjs` | 20/20通过 |
| 内容门禁 | `npm run quality:content` | 29/29通过 |
| 生产产物构建 | 以`sites-env.sh`同等运行时变量执行`node_modules/.bin/vinext.cmd build` | 通过 |
| 标准构建脚本 | `npm run build` | 当前Windows环境的`bash`映射到未初始化WSL，未运行成功；未将该平台阻塞记为通过 |
| 部署目标门禁 | `node --test tests/deploy-guard.test.mjs`与`node scripts/deploy-cloudflare.mjs --check` | 3/3通过；目标为`english-exam / zhenti-judu-prod`，检查未连接数据库或发布 |
| 差异与范围 | `git diff --check`（入口修复提交前） | 通过 |

下一步：若要让`english.onehjt.dpdns.org`同步主站版本，需要拥有Cloudflare账户路由或发布令牌的维护者将该域名路由到当前`english-exam` Worker，或在具备`CLOUDFLARE_API_TOKEN`的受控发布环境执行既有发布流程；不要通过改写试题内容、迁移数据库或绕过发布门禁来解决。Text 2只有拿到含完整并列对象的用户原卷后才能继续精审。
