# 待学 / 已学单词按日期归档

日期：2026-09-21

## 变更范围

- 将词汇首页的“今日新词目标 / 今日已完成”概览替换为“待学单词 / 已学单词”。
- 主操作改为“学习待学单词”，筛选标题同步改为“待学筛选范围”；每日数量设置与队列算法保持原样。
- “我的单词”分为待学和已学两个状态入口：待学按加入日期，已学按首次真实学习日期分组；每个规范义项只显示一次，保留全部年份和出处。
- 每个日期组都可点击标题折叠或展开；最新日期默认展开，较早日期默认收起，组内数量始终可见。
- 单纯在正文、题干或选项中标记的词仍是待学，只有真实阅读评分、`lastReviewedAt` 或旧版保留评分才能进入已学。旧记录缺少精确评分时间时，仅使用其可恢复的 `updatedAt` 归档。
- 已标记词组保留独立折叠入口，没有从原界面或学习队列中删除。

## 兼容边界

- 未增加或修改持久化字段；不迁移、不重写现有 memory、attempt、session、复习计划、笔记、清单或同步封包。
- 未修改文章、题目、句法、词条稳定键、登录、账号隔离、数据库和部署配置。

## 已执行检查

- 相关学习逻辑与UI专项：41项通过。
- 构建后UI、渲染与日期分类专项：29项通过。
- 日期折叠结构与已有词汇交互专项：14项通过。
- `npm run lint`：0错误；保留项目原有6条警告。
- `npm run quality:content`：213项通过；词义账本3,209词条、5,502行，重复释义标签为0，账本输入未变化。
- `npm run build`：通过，输出 `Build complete.`；非Cloudflare main环境按既有脚本跳过远端迁移。
- `git diff --check`：通过。

## 尚未替代的人工验证

- 自动检查不等于手机真机触控验证；发布后仍需在正式站确认两个状态切换、搜索和日期分组的实际交互。
