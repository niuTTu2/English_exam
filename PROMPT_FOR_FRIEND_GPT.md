# 可直接交给同事 Codex 的指令

你是“真题句读”的接续维护者。仓库为 `niuTTu2/English_exam`；用户已授权按篇完成真题精审、独立提交并推送 `main`，由既有 Cloudflare 流程自动部署，不再等待用户逐篇复核。

开始前完整阅读 `AGENTS.md`、`START_HERE.md`、`docs/CONTENT_QUALITY_STANDARD.md`、`docs/ARTICLE_IMPORT_TEMPLATE.md`、`docs/RELEASE_CHECKLIST.md`，再读取 `docs/IMPORT_PROGRESS.md` 的当前接续点和上一篇报告。以仓库数据与提交记录确认进度，不把“测试通过”当成语义准确性的证明。

最新优先级覆盖下文历史接续说明：先读`docs/TRAINING_TEMPLATE.md`和`docs/SYNTAX_UPGRADE_PROGRESS.md`。用户已确认手机交互完成，2010 Text 2已参照Text 1补齐整篇训练模板，报告为`docs/change-reports/2010-text2-training-template.md`。两篇各19句、35项句子任务、3项篇章回忆、5段与5题证据，并具备全部题干/选项语言分析。其余518句未迁移。后续从2010 Text 3接续，但不得把未接入草稿当作已审内容。先核对本次提交及部署记录，不重复导入Text 2。保留任务级提示、递进间隔、精确定位和历史；不恢复旧版粗粒度辅助逻辑。2012 Text 2早已由9757645提交。用户不逐篇人工复核，不再次要求审批已授权的提交。

先检查工作区和远端状态，保护他人未提交修改；仅在工作区干净时 `git pull --ff-only origin main`。2010年英语二全部阅读已完成：Text 1—4及Part B，共96句、25题，题号21—45连续覆盖。用户随后授权上传第46题英译汉，本次单独收录三段、10句、15分的整篇任务；不扩展写作或回到2001年。若台账仍记录提交前状态，先以Git历史和交付消息确认是否已推送，再核验公开生产页，不能重复导入。后续新任务仍需确认能读取用户整卷；原聊天附件不保证随仓库转交，不能用不可靠转录补齐。

2026-09-17新增授权：用户已提供2011年英语二整卷，当前转入完成2011全卷，包括写作；上一段的“不扩展写作”只约束当时2010任务。以台账为准接续，七选项对应题必须保留A—G，图表数据按用户原图可辨认的精度处理。

2011已全部交付。2026-09-17用户另行提供2012整卷并要求继续，授权范围含全部九模块。按台账接续2012，图表原图为公司员工工作满意度调查；不得复用2011图表的年份表头。

一次只做一篇。2000年完形保留词库完整性基线，新阅读以2010 Text 1训练模板为准，不以文字密度衡量质量。精确颜色/名称从同一人工成分产生；原卷分段、关系树、三层翻译、全文路线、多句题目证据、主动任务随文章一次完成。先解释关系再给术语，简单句只需一个关键任务，复杂句才完整分层。不要照搬上一篇可能存在的占位说明或错误。

重复词复用稳定原形；派生词只关联词族，不能把派生名词并入动词原形次数。同词在不同句子中含义不同时按 `articleId + sentenceId + headword` 保存语境。词组原文片段必须与正文一致，规范结构另存；已有规范模式优先复用。“本句可替换”需要完整、自然、保留原命题的改写与有效链接；没有可靠替换就不凑数。

句法新增要求：正文/翻译的 `beginnerSyntax.reading` 必须用一句关键点与1—3个带本句原文证据的解释，解决“为什么这样拆”。嵌套修饰用 `children` 表示，明确每个时间短语管哪个动作、从句内谁作主语、非谓语由谁发出。不得只换术语标签或套“结合上下文理解”；不得以截图中的AI讲解为免核验答案。参照2010 Text 1新版，既有内容未迁移部分见 `docs/SYNTAX_UPGRADE_PROGRESS.md`。

按精简审计执行：普通内容导入只运行 `npm run quality:content` 与 `npm run build`，不随后重复运行 `npm run quality` 或 `npm test`。失败时先汇总修复，再重跑受影响项。修改界面、运行逻辑、依赖或数据结构时先确认范围授权，再按发布清单追加对应检查。纯文档修改只检查差异与一致性。不得以“减少检查”为由删除测试、弱化断言、减少精审字段或隐藏缺项。

每篇使用 `docs/CHANGE_REPORT_TEMPLATE.md` 写一份报告，并更新台账。记录真实命令结果，未运行就写未运行。上下文中断时在台账记录篇目、完成阶段、疑点与下一步；接手后据此和 Git 状态恢复，不重做已完成内容。

只暂存本篇及必要接入文件，创建一个独立提交并推送；禁止强推、覆盖他人修改或改动账号、数据库、学习记录与部署配置。推送成功不等于部署成功：有权限则读取对应 SHA 的 Cloudflare 状态，否则记录部署未核验。权限失败或保护分支审批时停止并报告，不绕过。下一篇继续独立处理，不能混入本篇提交。

不索取、不提交密码、验证码、私钥或 API Key。不访问或修改杭州 ECS，不新增域名、公开访问范围或外部服务。
