# 可直接交给同事 Codex 的指令

你是“真题句读”的接续维护者。仓库为 `niuTTu2/English_exam`；用户已授权按篇完成真题精审、独立提交并推送 `main`，由既有 Cloudflare 流程自动部署，不再等待用户逐篇复核。

开始前完整阅读 `AGENTS.md`、`START_HERE.md`、`docs/CONTENT_QUALITY_STANDARD.md`、`docs/ARTICLE_IMPORT_TEMPLATE.md`、`docs/RELEASE_CHECKLIST.md`，再读取 `docs/IMPORT_PROGRESS.md` 的当前接续点和上一篇报告。以仓库数据与提交记录确认进度，不把“测试通过”当成语义准确性的证明。

先检查工作区和远端状态，保护他人未提交修改；仅在工作区干净时 `git pull --ff-only origin main`。当前交接点为2010年英语二阅读 Text 3（31—35），后续以台账为准。确认你能读取用户提供的整卷：原聊天附件不保证随仓库转交。没有原卷、答案依据不明或题文缺失时记录阻塞并询问，不凭记忆或来源不明的网络转录补齐。

一次只做一篇。精细程度不得低于2000年完形样板，句法使用后续确定的显式零基础拆解与渐进折叠。必须包含准确原句、可还原分块、只删减不改写的主干、具体修饰关系、从句内部主谓宾、直译与通顺译文、篇章逻辑；单词和词组有本句义、词性、变形、搭配与中文规则；题目有来源、定位和全部选项理由。不要照搬上一篇可能存在的占位说明或错误。

重复词复用稳定原形；派生词只关联词族，不能把派生名词并入动词原形次数。同词在不同句子中含义不同时按 `articleId + sentenceId + headword` 保存语境。词组原文片段必须与正文一致，规范结构另存；已有规范模式优先复用。“本句可替换”需要完整、自然、保留原命题的改写与有效链接；没有可靠替换就不凑数。

按精简审计执行：普通内容导入只运行 `npm run quality:content` 与 `npm run build`，不随后重复运行 `npm run quality` 或 `npm test`。失败时先汇总修复，再重跑受影响项。修改界面、运行逻辑、依赖或数据结构时先确认范围授权，再按发布清单追加对应检查。纯文档修改只检查差异与一致性。不得以“减少检查”为由删除测试、弱化断言、减少精审字段或隐藏缺项。

每篇使用 `docs/CHANGE_REPORT_TEMPLATE.md` 写一份报告，并更新台账。记录真实命令结果，未运行就写未运行。上下文中断时在台账记录篇目、完成阶段、疑点与下一步；接手后据此和 Git 状态恢复，不重做已完成内容。

只暂存本篇及必要接入文件，创建一个独立提交并推送；禁止强推、覆盖他人修改或改动账号、数据库、学习记录与部署配置。推送成功不等于部署成功：有权限则读取对应 SHA 的 Cloudflare 状态，否则记录部署未核验。权限失败或保护分支审批时停止并报告，不绕过。下一篇继续独立处理，不能混入本篇提交。

不索取、不提交密码、验证码、私钥或 API Key。不访问或修改杭州 ECS，不新增域名、公开访问范围或外部服务。
